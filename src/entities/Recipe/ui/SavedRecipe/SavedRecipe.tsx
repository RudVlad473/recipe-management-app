import { TRecipeCard } from "../../lib/types"
import React, { FC } from "react"

type SavedRecipeProps = TRecipeCard

export const SavedRecipe: FC<SavedRecipeProps> = ({ description, id, title, imageUrl }) => {
  return <div> </div>
}
